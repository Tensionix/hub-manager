# TECH_SPEC — Demo Full Project

Expected MIRROR behavior:

- copy `README.md`, `TECH_SPEC.md`, `system_core/app/main.py`, `config/demo.json`;
- preserve `logs/`, `output/`, `backup/`, `runtime/`, `wheelhouse/` as empty folders with `.gitkeep`;
- skip runtime/log/output contents if later added.
